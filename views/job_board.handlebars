
<div class="container">
{{#each jobs as |job| }}
<style>
div {
  padding: 10px;
}
</style>
<div class="accordion" id="accordion_{{@index}}">
  <div class="accordion-item">
    <h2 class="accordion-header" id="headingOne">
      <button class="accordion-button " type="button" data-bs-toggle="collapse" data-bs-target="#collapseOne_{{@index}}" aria-expanded="true" aria-controls="collapseOne_{{@index}}">
        <strong>  {{job.job_title}} </strong>
      </button>
    </h2>
    <div id="collapseOne_{{@index}}" class="accordion-collapse show" aria-labelledby="headingOne" data-bs-parent="#accordionExample" style="">
      <div class="accordion-body">
        <strong>Location: </strong> {{job.job_city}}<br>
        <strong>Employer: </strong> {{job.employer}}<br>
        <strong>Date Posted:</strong> {{format_date job.post_date}}
      </div>
    </div>
  </div>
  <div class="accordion-item">
    <h2 class="accordion-header" id="headingTwo">
      <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapseTwo_{{@index}}" aria-expanded="false" aria-controls="collapseTwo_{{@index}}">
        <strong>Detailed Job Description</strong>
      </button>
    </h2>
    <div id="collapseTwo_{{@index}}" class="accordion-collapse collapse " aria-labelledby="headingTwo" data-bs-parent="#accordionExample" style="">
      <div class="accordion-body">
        {{job.description}} 
      </div>
    </div>
  </div>
  <div class="accordion-item">
    <h2 class="accordion-header" id="headingThree">
      <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapseThree_{{@index}}" aria-expanded="false" aria-controls="collapseThree_{{@index}}">
       <strong> Apply if you're interested!</strong>
      </button>
    </h2>
    <div id="collapseThree_{{@index}}" class="accordion-collapse collapse" aria-labelledby="headingThree" data-bs-parent="#accordionExample" style="">
      <div class="accordion-body">
        <div class="d-grid gap-2">
          <button class="btn btn-success" id = "btn_{{job.job_id}}" data-id="{{job.job_id}}"><strong>Apply For Position</strong></button>
        </div>
      </div>
    </div>
  </div>
</div>
{{/each}}
</div>

<script src="./js/apply.js"></script>

